apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: crowdscope-docs
spec:
  rules:
    - host: docs.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: crowdscope-docs
                port:
                  number: 80
